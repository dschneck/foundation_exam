// Pre-processor directives
#include <stdio.h>
#include <stdlib.h>

// Struct definitions
typedef struct Node Node;

struct Node {
	int val;
	Node * next, prev;
};

// Function prototypes

// Main method

// Function definitons
